result := "";
for (p in [1..9]) {
    for (q in [1..9]) {
        if (p*q < 10) {
            result += "  " + p*q;
        } else {
            result += " " + p*q;
        }
    }
    result += "\n";
}
print(result);
