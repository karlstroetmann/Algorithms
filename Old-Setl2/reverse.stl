program main;

    L := [1..5];
    R := reverse(L);
    print(R);

    procedure reverse(L);
        if L = [] then
            return L;
        end if;
        x := L(1); R := L(2..);
        return reverse(R) + [x];
    end reverse;
end main;
